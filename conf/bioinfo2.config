// Configuração específica do servidor bioinfo2
// Ajustar após: nproc && free -h

params {
    t = 32
}

process {
    withLabel: 'process_low'    { memory = '8 GB'  }
    withLabel: 'process_medium' { memory = '32 GB' }
    withLabel: 'process_high'   { memory = '64 GB' }
}
